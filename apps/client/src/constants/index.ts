/**
 * ToolHub Client Constants
 */

export const SERVER_PORT = 3001;
export const SERVER_URL = `http://localhost:${SERVER_PORT}`;

export const API_BASE = `${SERVER_URL}/api`;
export const MOCK_BASE = `${SERVER_URL}/mock`;
